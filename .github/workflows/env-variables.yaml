name: env-variable-demo
on: workflow_dispatch
env:
 App_data: "DEV"

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
    - name: use of env variable
      env:
        App_date: "Prod"
       run: echo "we use $App_data"
