name: outputs-demo
on: push
jobs:
 job1:
  runs-on: ubuntu-latest
  steps:
   - name: step one
     id: 123
     run: echo "version=1.0.1" >> $GITHUB_OUTPUT
   - name: step two
     run: echo "This program version is: ${{ steps.123.outputs.version}}"
     
